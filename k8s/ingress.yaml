apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: travel-flight-fe-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: 2306209681-fe.hafizmuh.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: travel-flight-fe-service
                port:
                  number: 80
